---
summary: "enrichment adoption — ranked-feed and search tools surface result context (counts, query echo, empty-result guidance) in a typed enrichment block reaching both structuredContent JSON and content[] markdown trailer"
breaking: false
security: false
---

# 0.5.7 — 2026-05-30

## Changed

- **`hn_get_stories`** — `total`, `offset`, `hasMore` moved from `output` to `enrichment`; `notice` (empty-result guidance) added to `enrichment`. `structuredContent` field `offset` renamed to match its new location; value preserved.
- **`hn_get_thread`** — `totalLoaded`, `totalAvailable` moved from `output` to `enrichment`; `omitted` object replaced by `notice` string in `enrichment` (consolidates deleted/dead counts and pagination hint into one field).
- **`hn_search_content`** — `totalHits`, `page`, `totalPages` moved from `output` to `enrichment`; `message` renamed to `notice` in `enrichment`. `query` stays in `output`.
- **Tests** — updated across all three tool test files to match the revised `output` + `enrichment` shapes.
- **`@cyanheads/mcp-ts-core` ^0.9.13 → ^0.9.16** — enrichment API adoption.
- **Skill sync** — bulk refresh across `add-tool`, `add-app-tool`, `add-service`, `api-context`, `api-linter`, `design-mcp-server`, `git-wrapup`, `maintenance`, `polish-docs-meta`, `setup`; `devcheck` skill removed (superseded by the `devcheck.config.json`-driven script).
