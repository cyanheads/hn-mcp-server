---
summary: "Maintenance. `@cyanheads/mcp-ts-core ^0.9.1 → ^0.9.6`, `zod` added as explicit dependency. `manifest.json` + `.mcpbignore` scaffolded for MCPB bundle support. Install badges added to README. New scripts: `list-skills`, `lint:packaging`, `bundle`, `audit:refresh`."
breaking: false
security: false
---

# 0.5.4 — 2026-05-23

## Maintenance

- `@cyanheads/mcp-ts-core ^0.9.1 → ^0.9.6`. Span across five patch releases; no breaking changes for this server.
- `zod ^4.4.3` added as an explicit dependency (previously transitive via mcp-ts-core).
- `manifest.json` scaffolded — MCPB bundle descriptor for Claude Desktop one-click install.
- `.mcpbignore` scaffolded — excludes source, devDeps, and scripts from the bundle.
- `package.json` scripts: `list-skills`, `lint:packaging`, `bundle` (`@anthropic-ai/mcpb pack`), `audit:refresh`.
- `scripts/lint-packaging.ts` + `scripts/list-skills.ts` added (synced from mcp-ts-core).
- `scripts/devcheck.ts` resynced from mcp-ts-core.
- README: install badges row (Claude Desktop MCPB, Cursor deeplink, VS Code deeplink). Docker config snippet added to self-hosted section. Badge row reordered; description line tightened.
- CLAUDE.md: `ctx.tenantId` row added to Context table; `list-skills`, `lint:packaging`, `bundle`, `audit:refresh` added to Commands table.
