---
summary: "hn_get_user: surface submission pagination notice via enrichment block"
breaking: false
security: false
---

# 0.5.9 — 2026-06-04

## Fixed

- **`hn_get_user` pagination notice** — adds `enrichment.notice` and calls `ctx.enrich.notice()` when `includeSubmissions` is true and the resolved (live) submission count is less than `totalSubmissions`. The notice reads: `Showing N of M submissions. Raise submissionCount (max 50) for more.` Resolves [#8](https://github.com/cyanheads/hn-mcp-server/issues/8).
